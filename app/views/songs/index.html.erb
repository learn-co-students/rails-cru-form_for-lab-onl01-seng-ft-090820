<% @songs.each do |song| %>
<div>
<p><%= link_to song.name, song_path(song.id) %></p>
<p><%= link_to song.genre.name, genre_path(song.genre.id) %></p>
<p><%= link_to song.artist.name, artist_path(song.artist.id) %></p>
</div>
<% end %>